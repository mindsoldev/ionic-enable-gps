import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hu.mindsol.gps',
  appName: 'ionic-enable-gps',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
